# Progress log

## Phase 0 — Bootstrap ✅
- `git init` on branch `main`; scaffolded the full repository structure (§4 of the brief).
- Wrote `requirements.txt` (pinned via `pip freeze` of the actually-installed
  versions), `README.md`, `CLAUDE.md`, `Makefile` (setup/test/lint/run/figures/
  reproduce/clean), `.gitignore`, MIT `LICENSE`, and `pyproject.toml` (src layout,
  editable install, pytest config).
- Created `.venv`; installed deps; `pip install -e .` makes `canyon_lbm` importable.
- **AC met:** `make setup` builds the env; `make test` runs; `git status` clean after commit.

## Phase 1 — Core solver + canonical checks ✅
- Implemented D2Q9 BGK in `src/canyon_lbm/lattice.py`: velocity set + weights +
  opposites, second-order equilibrium, force-corrected macroscopic moments,
  Guo (2002) forcing, BGK collision, periodic streaming, ν↔τ mapping.
- Implemented halfway bounce-back via a boolean solid mask in
  `src/canyon_lbm/boundary.py` (plus Zou/He inlet, zero-gradient outflow, and
  free-slip top, documented and ready for Phase 2 validation).
- `src/canyon_lbm/solver.py`: `LatticeUnits` (explicit lattice↔physical mapping)
  and `run_poiseuille` (force-driven channel, steady-state detection).
- **Poiseuille validation (AC: within ~1%):**
  - relative L2 error = **7.9e-4** (≈0.08%), max relative error = **5.8e-4**
    (run_case defaults, ny=32); finer ny=42 grid gives **4.5e-4** L2.
  - mass conserved to **< 1e-9** (relative) over the run.
  - converged to steady state in ~1.8e4 iterations.
- **Tests:** 15 pytest tests pass (equilibrium moments, mass conservation,
  ν↔τ roundtrip, bounce-back bookkeeping + no-slip symmetry, Poiseuille profile,
  COST 732 metrics).
- Figure `figures/poiseuille_validation.{png,svg}` regenerated by
  `scripts/make_figures.py`; result + metadata sidecar in `results/`.

## Phase 2 — Canyon geometry + boundary conditions ✅
- **Config-driven geometry** (`geometry.build_canyon`): building height H, street
  width W = H/AR, building width, COST 732 margins -> solid mask + index sets
  (street, buildings, roof row, cavity, source row). Tested across aspect ratios.
- **Boundary conditions** (all wired into `solver.CanyonSimulation`):
  - bounce-back walls (ground + buildings);
  - free-slip top (specular via post-collision f) -- validated independently
    against the half-channel analytic profile (zero penetration, zero top shear);
  - non-equilibrium-extrapolation velocity inlet (log-law profile) + constant-
    pressure outlet -- replaced Zou/He, which seeded a corner instability;
  - **outlet viscosity sponge** to absorb the reflective-BC acoustic standing
    wave (a spatially-varying tau field);
  - inlet **ramp** + rest start to avoid the startup shock.
- **Unsteadiness handled by time-averaging**: the canyon flow sheds vortices
  above Re ~ O(100), so `run()` reports the time-averaged mean field (the
  machinery the production LES runs will reuse). Low Re (<~70) is steady.
- **H/W = 1 demonstration** (`configs/canyon_demo.yaml`, Re = 100, BGK + sponge,
  time-averaged over 60k steps): a clean **single clockwise vortex** —
  cavity circulation = -0.078 (clockwise), one centreline u_x reversal, up the
  windward wall / down the leeward wall, downstream aloft / return flow at the
  floor; **mass imbalance = 0.15%** (inflow ≈ outflow). Figure
  `figures/canyon_flow_HW1.{png,svg}` from `scripts/make_figures.py`.
- **Tests:** 27 pass (added geometry, free-slip, and canyon stability/mass/
  single-vortex/config-driven tests).

## Phase 3 — Passive-scalar pollutant + ventilation metric ✅
- **D2Q5 advection-diffusion scalar** (`scalar.py`): linear ADE equilibrium,
  one-way coupled to the flow, `tau_g` from the Schmidt number; zero-flux walls,
  clean-air (C=0) inlet, open outlet/top, continuous street-floor line source.
- **Verified on analytic cases** (the Phase 3 gate):
  - pure diffusion: variance grows as `var0 + 2Dt` to **0.0%** error, mass
    conserved to machine precision;
  - uniform advection: centre of mass translates **exactly** at u;
  - closed-box source injects at exactly the prescribed rate.
- **Metrics** (`metrics.py`): retention (canyon-mean C), normalized air-exchange
  index ACH* = w_e/U_H = Q·H/(content·U_H), total roof-opening flux
  (advective + diffusive), and CODASC c+.
- **H/W = 1 demo** (`configs/canyon_demo.yaml`, Re=100, scalar, time-averaged):
  - flow stays a clean single clockwise vortex (circulation −0.062, mass
    imbalance 0.09%) with the scalar coupled;
  - canyon pollutant content **plateaus** (rose smoothly to ~6.4e5 then flat to
    <0.3%) → **the metric is stable once equilibrated** (the AC);
  - roof-opening flux balances the source to ~92% (advective ~5 + diffusive ~18
    ≈ source 24) — conservation confirmed; the exchange is **diffusion-dominated**
    at this laminar Re (mean roof vertical velocity ~0 in the recirculation);
  - figure `figures/canyon_concentration_HW1.{png,svg}` shows the pollutant
    trapped on the **leeward wall + street floor** (the classic skimming pattern).
- **Tests:** 36 pass (added D2Q5 ADE verification, source injection, coupled
  canyon scalar, and ventilation-metric tests).

## Phase 4a — High-Re infrastructure ✅ (commit 3bf9552)
- **MRT** collision (Lallemand–Luo moments; reduces to BGK exactly, conserves
  mass+momentum — tested) and **Smagorinsky LES** eddy viscosity, in `lattice.py`.
- **CuPy GPU backend** (`backend.py`): NumPy stays the validated reference; CuPy
  reproduces it to round-off (rel ~1e-13). ~13x speedup at 96 cells/H on an
  RTX 4090. Optional dep (`pip install -e .[accel]`).

## Phase 4b — Grid-independence (HARD gate): regime settled
A long diagnostic effort established **where a clean grid-independence gate can
exist** (full detail in DECISIONS.md D18):
- **High Re (1e4, 2000) + MRT+LES is ruled out**: n=96 blow-up at the τ→0.5
  stability edge; the poorly-ventilated canyon never equilibrated at the low Mach
  it needed; and — decisively — **LES grid-independence is ill-posed** (the
  sub-grid filter ∝ cell size, so refining changes the effective Re; the metric
  moved 3× from n=24→n=48).
- **Transitional laminar (Re=150) is also ruled out**: the canyon **sheds above
  ~Re 40** (≈ the Re 47 bluff-body onset; confirmed — n=96 stays unsteady at
  Re=40/150). Coarse grids numerically damp the shedding into a *fake* steady
  state while fine grids resolve it, so the metric **diverges** with refinement
  (negative Richardson order).
- **Resolution (user-confirmed): steady laminar Re=25, BGK, no LES.** Below the
  shedding onset → steady and fully resolved at every grid → grid-converges
  cleanly. The brief's sanctioned **Plan B** (2-D laminar proof-of-method);
  Péclet ≈ 18 (advection-dominated, shows the H/W contrast); turbulence/high-Re/
  3-D are stated limitations with no quantitative real-canyon claims.
- The MRT + GPU infrastructure from 4a still accelerates the (laminar) production
  sweep; the sponge + time-averaging carry forward.
- **PASSED:** retention 298.8/299.0/299.0 at 24/48/96 cells/H, **48→96 = 0.0%**
  (< 3% gate). Clean clockwise single vortex at every grid; ~equilibrated
  (flux/source ≈ 1.0). **Production resolution = 48 cells/H.** Figure
  `figures/grid_independence.png`.

## Phase 5 — CODASC validation: qualitative (gate reframed, Plan B)
- Downloaded the CODASC reference (AR=1, no trees, 90°; KIT, via
  umweltaerodynamik.de) to `data/validation/` (git-ignored; see its README).
- The steady-laminar model **captures the vertical c+ decay** but, at the
  physical Schmidt number (Pe≈18, diffusion-dominated), **misses the
  leeward/windward asymmetry** (ratio 1.02 vs CODASC 3.0). Raising the Péclet
  number recovers it partially (1.69 at Pe=200) — **confirming the asymmetry is
  an advection/Péclet effect** — but the weak laminar vortex can't fully match
  the turbulent tunnel. FAC2≈0.79 is coincidental, not a real pass (D19).
- **Outcome (user-confirmed): 2-D laminar proof-of-method.** Quantitative
  turbulent validation is a stated limitation; the Péclet-controlled asymmetry
  is the validation insight. Figure `figures/codasc_validation.png`.

## Phase 6 — production H/W sweep (idealized laminar exploration) ✅
Swept H/W ∈ {0.5, 0.66, 1.0, 1.5, 2.0, 2.5, 3.0} at 48 cells/H, steady-laminar
Re=25, BGK, Pe=50; metric is the fill-independent equilibrium retention
(retention/flux_over_source) + the air-exchange rate ACH = flux/content. Saved via
`io.save_result` (sidecar); scalar grid-check at H/W=1 confirmed n=48→96 = 1.1%.
**Honest finding (trends/mechanisms only — no absolute real-canyon values):**
- The **flow regime-change IS captured**: the cavity vortex weakens monotonically
  with H/W (circulation −0.74 at H/W=0.5 → −0.04 at H/W=3).
- But the **pollutant ventilation does NOT show the Oke skimming collapse**: in
  the grid-converged laminar regime the canyon-top exchange is diffusion-
  dominated, so retention is weak/non-monotonic (~9% range, peak near H/W=1) and
  ACH is U-shaped (worst ventilation near H/W=1) — *not* the monotonic collapse
  of real turbulent canyons.
- **Why (the HEADLINE result):** the trapping/asymmetry is **advection (Péclet)
  controlled** — `scripts/pe_sensitivity.py` shows the leeward/windward asymmetry
  rising from ~1.0 (Pe=18, symmetric) toward the CODASC value as Pe increases.
  At the Péclet numbers a grid-converged 2-D laminar model can reach, advection
  is too weak to reproduce the collapse.
- **Methods finding:** `figures/les_grid_divergence.png` documents why 2-D LES
  cannot rescue this — the metric *diverges* on refinement (2.9× drift n=24→48),
  vs the laminar convergence. (D18, D20.)

## Phase 6.5 — high-Péclet, grid-checked H/W diagnostic ✅ (OUTCOME A; D21)
Phase 6 swept at **Pe=50** — below the advection-dominated regime — so its "no
collapse" was untested at high Péclet. Phase 6.5 closes that gap, **staying inside
the locked regime** (steady-laminar Re=25, BGK, no LES/RANS): only the scalar
diffusivity changes. Exploiting the one-way coupling (D13), the steady flow is
solved **once per H/W** and reused across the Pe ladder. `scripts/phase6_5_peclet.py`.
- **The skimming collapse IS reproduced once advection dominates.** Retention rises
  **monotonically** with H/W at Pe≥144 (flat/non-monotonic at Pe=50); ACH\* falls.
  At Pe=200: retention_eq = 1721 / 2510 / 3596 / 3571 for H/W = 0.5 / 1 / 2 / 3
  (saturating ~H/W 2–3). Phase 6.5 **refines, not contradicts**, Phase 6.

  | H/W | Pe=50 | Pe=72 | Pe=144 | Pe=200 |
  |----:|------:|------:|-------:|-------:|
  | 0.5 |  760  |  964  |  1431  |  1721  |
  | 1.0 |  756  | 1053  |  1926  |  2510  |
  | 2.0 |  726  | 1053  |  2318  |  3596  |
  | 3.0 |  702  | 1025  |  2282  |  3571  |
  (equilibrium retention; the monotonic rise with H/W = the collapse, emerging as Pe↑)

- **Grid check (HARD gate), Pe=200, H/W=1, n=24/48/96 — PASSED:** retention_eq
  **0.2%**, ACH\* **0.2%**, asymmetry **0.7%** (n=48→96), observed order p≈+5.
  The naive per-step ACH "49.9%" was a **units artifact** (ACH[1/step] ∝ dt ∝ 1/n,
  halves by construction); the grid-invariant **ACH\* = ACH·H/u_lbm** converges.
  | n | retention_raw | retention_eq | ACH\* | Pe_cell |
  |--:|--------------:|-------------:|------:|--------:|
  | 24 | 948.1 | 2656 | 0.00753 | 8.3 |
  | 48 | 944.6 | 2510 | 0.00797 | 4.2 |
  | 96 | 947.4 | 2505 | 0.00798 | 2.1 |
- **Grid-converged Péclet ceiling ≥ 200** (at 96 cells/H). The integral metrics
  converge even at Pe_cell≈2–4 — under-resolution perturbs only the thin per-cell
  roof flux, not the bulk canyon mean (retention is within 0.3% already at n=48).
- **Decisive figure** `figures/peclet_hw_diagnostic.png`; data
  `results/phase6_5_peclet.{json,summary.csv}` (sidecar via `io.save_result`).
- **Verdict: OUTCOME A** — the collapse is reproduced and grid-converged in the
  advection-dominated regime; the **Péclet number is the controlling parameter**.
  No turbulence reopened (D18/D20 stand).

## Phase 6.5b — clean-resolution (Pe_cell<2) confirmation: MIXED RESULT (D22)

**Purpose:** Phase 6.5's grid check sat at Pe_cell≥2 (borderline) across all three
grids. Phase 6.5b tested whether the Pe=144 headline metrics converge when BOTH
grids are unambiguously resolved (Pe_cell<2): n=96 (Pe_cell=1.50) and n=192
(Pe_cell=0.75). The flow was solved as a **time-averaged mean** (BURN_FT=1.5 +
AVG_FT=3.5 ≈ 10 acoustic periods), after discovering that Re=25 carries a residual
acoustic mode (D22A) that prevents tol-convergence; the mean is window-independent
to <0.5% at ≥10 periods. GPU (RTX 4090, CuPy, float64). All 8 flow+scalar cases
completed; n=192 flow solves cached in `results/_flowcache/`.

**Pre-run diagnostics (D22A):** `scripts/diagnose_unsteadiness.py` — 8-case sweep
confirmed Re=25 oscillation is a **residual acoustic mode, not physical shedding**
(Mach sweep: St∝1/u; domain-resonant; sponge-insensitive; T∝n). "Steady laminar
Re=25" label corrected to "time-averaged (statistically-stationary) laminar Re=25"
throughout.

**Pe=144 results (n=96 Pe_cell=1.50 | n=192 Pe_cell=0.75):**

| H/W | ret_eq n=96 | ret_eq n=192 | Δret_eq | ΔACH* | Δasym |
|----:|------------:|-------------:|--------:|------:|------:|
| 0.5 | 1340.8      | 1125.9       | 16.03%  | 19.09% | 6.18% |
| 1.0 | 1963.4      | 1964.2       | 0.04%   | 0.04%  | 5.11% |
| 2.0 | 2309.1      | 2227.1       | 3.55%   | 3.68%  | 0.77% |
| 3.0 | 2246.4      | 2092.9       | 6.83%   | 7.34%  | 0.05% |

Worst change: **19.09%** (H/W=0.5, ACH*). Target was <1%.

**VERDICT: <1% criterion FAILS for H/W∈{0.5, 2, 3}. Outcome A NOT locked as
fully grid-converged across all H/W.** H/W=1.0 is a clean exception (0.04%).

The collapse *direction* is robust at both grids (monotonic rise H/W=0.5→2,
saturation H/W=2→3). The qualitative Pe-controls-the-collapse finding is intact.
The n=192 curve (Pe_cell=0.75) is the best-resolved estimate: 1126/1964/2227/2093
for H/W=0.5/1/2/3.

Root cause of non-convergence: BGK τ_flow differs between grids (1.076 at n=96 vs
1.652 at n=192) to maintain Re=25, giving different numerical dissipation; the n=192
vortex is proportionally weaker (circ ratio 1.61 vs expected 2.0 at H/W=0.5).

Pe=200 follow-on (optional, cached flows): H/W=1 Δret=0.85% (borderline), H/W=2
Δret=4.34% (fail). Same pattern.

Data: `results/phase6_5b_peclet.{json,summary.csv}`; D22 in DECISIONS.md.

## Phase 6.5c — flow-convergence fix attempt: TRT magic-parameter, NO-GO (D23)

**Hypothesis:** the D22B Pe=144 non-convergence (16–19% n=96→192 at H/W=0.5) is a
FLOW problem — BGK's bounce-back wall location is τ-dependent (magic parameter
Λ=(τ−½)² = 0.33 at n=96 vs 1.33 at n=192), so the two grids solve subtly different
geometries (cavity-circulation ratio 1.61× vs the pure-scaling ideal 2.0×).

**Test:** re-solve the flow as pure TRT with the bounce-back-consistent magic
parameter Λ=3/16 (τ-independent wall; viscosity/Re=25 unchanged — only the odd
q-rate moves), H/W=0.5 (worst case), n=96 & n=192, Pe=144. Implemented via the
Phase-4a MRT routine (`scripts/phase6_5c_mrt_test.py`).

**Result — NO-GO, wall hypothesis REFUTED:**

| metric | BGK n=96→192 | TRT n=96→192 |
|---|---|---|
| circ ratio | 1.614× | 1.608× |
| retention_eq Δ | 16.03% | 16.01% |
| ACH* Δ | 19.09% | 19.07% |

The TRT flow is identical to BGK (circ differs 0.6% despite Λ changing 7×).
Pinning the wall does essentially nothing — at Re=25 the cavity circulation is set
by the large-scale recirculation, not sub-cell wall slip. Averaging window was
13–17 periods at both grids (not a confound either).

**Honest fallback (adopted):** report **n=192 as the best-resolved estimate**; the
residual grid-sensitivity at low H/W is real but is **NOT a wall-BC artifact** (now
tested, not assumed). The **directional skimming collapse is robust** at both grids
and both collisions (BGK, TRT); the Péclet-controls-the-collapse headline (D21)
stands. Only the "<1% grid-converged at every H/W" claim is retired (holds at H/W=1;
elsewhere a stated few-to-~16% band). Full MRT re-run CANCELLED (would reproduce
BGK). Candidate true cause (untested, user stopped on NO-GO): wide-canyon vortex
still resolving and/or residual scalar numerical diffusion. Data:
`results/phase6_5c_mrt_test.json`. `results_limitations_framing.md` NOT touched.

## Future work (recorded, not built — D20)
A grid-convergent turbulent canyon needs **steady RANS (k-ε / k-ω)**, the closure
CODASC-validated studies use; not in this LBM codebase (major build), and 2-D
RANS still misses 3-D along-canyon exchange. 3-D is the real next step.

## Remaining: Phases 7-9
Figure set (≥5, all regenerable via `scripts/make_figures.py`), IMRaD paper
draft, reproducibility audit (`make reproduce`).
